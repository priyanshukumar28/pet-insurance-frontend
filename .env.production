# Standalone admin deploy (Vercel project → pet-insurance-frontend.vercel.app).
# Served at the domain root, so no base path. /api and /uploads are proxied to
# the hosted backend by vercel.json — keeping the auth cookie first-party so a
# token refresh never logs you out.
VITE_API_URL=/api
VITE_BASE=/
