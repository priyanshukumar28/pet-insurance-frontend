# Local dev — point at your local backend (see backend/.env PORT).
VITE_API_URL=http://localhost:5001/api

# Production build uses .env.production instead:
#   VITE_API_URL=https://pet-insurance.infotechclinic.com/api
